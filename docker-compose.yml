x-environment-variables: &common-env-vars
  POSTGRES_DB: letters
  POSTGRES_USER: user
  POSTGRES_PASSWORD: password

services:
  app:
    build:
      context: https://github.com/anand-san/letter-management-api.git
      dockerfile: Dockerfile
    env_file:
      - .env.app.prod
    ports:
      - "8004:8000"
    environment:
      <<: *common-env-vars
      POSTGRES_HOST: db
    depends_on:
      - db
    networks:
      - letter-management-app-network
    restart: always

  db:
    image: postgres:16
    environment:
      <<: *common-env-vars
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - letter-management-app-network
    restart: always

volumes:
  postgres-data:

networks:
  letter-management-app-network:
